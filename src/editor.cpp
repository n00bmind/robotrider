void
UpdateAndRenderEditor( GameMemory *memory, GameRenderCommands *renderCommands )
{
    PushClear( renderCommands, { 0.95f, 0.95f, 0.95f, 1.0f } );

    float width = renderCommands->width;
    float height = renderCommands->height;
}

